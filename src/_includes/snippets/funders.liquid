<section class="bg-white border-top flex-col flex-center section section--quarter">
  <h2 class="label align-center">Funded by</h2>
  <ul class="unstyled-list flex-center">
    {% for funder in funders %}
    <li>
      <a href="{{ funder.url }}" target="_blank" rel="noopener" title="Go to {{ funder.name }}’s site" class="flex-center">
        {% include {{ funder.logo }} %}
      </a>
    </li>
    {% endfor %}
  </ul>
</section>
