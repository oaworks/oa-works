<section class="bg-white border-top section" data-sal="slide-up">
  <h2 class="font-serif label flex-center">Covered in</h2>
  <ul class="row row--centered flex-center unstyled-list">
    {% assign delay_count = 0 %}
    {% for item in coverage %}
    {% assign delay_count = delay_count | plus: 100 %}
    <li class="col col--1of6" data-sal="slide-up" data-sal-delay="{{ delay_count }}">
      <a href="{{ item.url }}" title="{{ item.name }}’s site" target="_blank" rel="noopener" class="flex-center bw">
        {% include item.logo %}
      </a>
    </li>
    {% endfor %}
  </ul>
</section>
